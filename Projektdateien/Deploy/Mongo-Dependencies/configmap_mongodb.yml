apiVersion: v1
kind: ConfigMap
metadata: 
  namespace: rocketchat
  name: mongodb-config
immutable: false
data:
  mongodb.conf: |
    storage:
      dbPath: /data/db
    replication:
      replSetName: "rs0"
    security:
      authorization: enabled
      keyFile: /etc/mongo-keyfile/keyfile.txt